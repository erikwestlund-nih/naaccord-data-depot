# Cloudflare real client IP configuration
# This file is generated by Ansible from deploy/ansible/roles/docker_services/templates/05-real-ip-cloudflare.conf.j2
# Source ranges: https://www.cloudflare.com/ips

real_ip_header CF-Connecting-IP;
real_ip_recursive on;

# Cloudflare IPv4 ranges
{% for cidr in cloudflare_ipv4_cidrs %}set_real_ip_from {{ cidr }};
{% endfor %}

# Cloudflare IPv6 ranges
{% for cidr in cloudflare_ipv6_cidrs %}set_real_ip_from {{ cidr }};
{% endfor %}
